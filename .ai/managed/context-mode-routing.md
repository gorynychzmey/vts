<!-- internal-standards:managed -->
# Context-Mode Routing

- Use context-preserving tooling when available instead of dumping large shell output into chat.
- Prefer batch gathering and targeted follow-up queries for analysis work.
- Keep responses compact and write substantial artifacts to files.
- After context compaction, session resume, or handoff, reread `.ai/managed/shared-rules.md` first and then reopen linked tool-specific/shared references only as needed.
- Reuse the shared routing block for both Claude and Codex surfaces.
